{
	"name": "jareddgotte/mcm",
	"description": "Movie Collection Manager (MCM), a legacy PHP movie-collection manager. This manifest is for development tooling only; the site itself installs no Composer package and loads only the class autoloader Composer generates for its own files.",
	"license": "Apache-2.0",
	"type": "project",
	"require": {},
	"autoload": {
		"classmap": [
			"inc/classes/",
			"inc/libs/"
		]
	},
	"require-dev": {
		"php": ">=8.1.0 <8.5.0",
		"phpunit/phpunit": "12.5.33"
	},
	"config": {
		"autoloader-suffix": "mcm",
		"platform": {
			"php": "8.3.0"
		},
		"sort-packages": true
	}
}
